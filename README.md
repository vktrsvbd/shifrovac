# shifrovac
uloha na OOP

                    (APP Sifrovac)
                        (main)
                          |
                     {Class sifra}
                          |           
{Class sifra1} <- {Class(de)serializace} -> String || soubor
                                          
